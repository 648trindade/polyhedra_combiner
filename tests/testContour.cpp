#define CATCH_CONFIG_MAIN // This tells Catch to provide a main() - only do this in one cpp file
#define CATCH_CONFIG_CONSOLE_WIDTH 120
#include <catch2/catch.hpp>

#include "../Contour.h"

TEST_CASE("Add edge")
{
    REQUIRE(false);
}

TEST_CASE("Overlap point")
{
    REQUIRE(false);
}

TEST_CASE("Get center")
{
    REQUIRE(false);
}

TEST_CASE("Is convex")
{
    REQUIRE(false);
}